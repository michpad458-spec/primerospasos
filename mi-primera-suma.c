//tipos de datos numericos enteros
#include <stdio.h>  
int main() {
    int a = 5; //declaracion de una variable entera
    int b = 10; //declaracion de otra variable entera
    int suma = a + b; //operacion de suma entre las dos variables
    printf("La suma de %d y %d es: %d", a, b, suma); //imprime el resultado de la suma en pantalla
    return 0; //indica que el programa termino correctamente
}